import { Button } from "@/components/ui/button";
import Image from "next/image";

const Home = (): React.ReactElement => {
  return (
    <p className="mx-auto w-full text-center py-[200px] ">
      <Button>Hello world!</Button>
    </p>
  );
};

export default Home;
